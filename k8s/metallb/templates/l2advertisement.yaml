apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: {{ .Values.namePrefix }}-l2
  namespace: metallb-system
spec:
  ipAddressPools:
    - {{ .Values.namePrefix }}-pool
  interfaces:
    - enp1s0