apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "awroberts-web.fullname" . }}-test-connection"
  labels:
    {{- include "awroberts-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "awroberts-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
