apiVersion: v1
kind: Service
metadata:
  name: {{ include "awroberts.fullname" . }}-background
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: "awroberts-web-background"
    app.kubernetes.io/instance: "awroberts-web"

  ports:
    - name: http
      port: 80
      targetPort: 80
